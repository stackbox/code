#include <iostream>
using namespace std;

int main()
{
    int k,i;
    while(cin >> k) {
	for(i=0;i<=65;i++) {
	    int sum= 18+i*k;
	    if(sum%65==0) break;
	}
	if(i==66) cout << "no" << endl;
	else  cout << i << endl;
    }
    return 0;
}

